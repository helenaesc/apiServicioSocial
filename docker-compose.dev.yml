services:
  db:
    image: mysql:8.0
    container_name: poncho_db_dev
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: poncho
    ports:
      - "3307:3306"
    volumes:
      - db_data_dev:/var/lib/mysql
      - ./sql/final:/docker-entrypoint-initdb.d

  backend:
    build: .
    container_name: poncho_backend_dev
    restart: always
    depends_on:
      - db
    environment:
      DB_HOST: db
      DB_PORT: 3306
      DB_USER: root
      DB_PASSWORD: rootpass
      DB_NAME: poncho
      PORT: 5000
      FLASK_ENV: development
      PYTHONDONTWRITEBYTECODE: 1
      PYTHONUNBUFFERED: 1
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    command: python main.py

volumes:
  db_data_dev: